<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ══════════════════════════════════════════════════════
         Qualification Master — List (inline editable)
    ══════════════════════════════════════════════════════ -->
    <record id="view_edu_qualification_tree" model="ir.ui.view">
        <field name="name">edu.qualification.tree</field>
        <field name="model">edu.qualification</field>
        <field name="arch" type="xml">
            <list editable="bottom">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="active" column_invisible="True"/>
            </list>
        </field>
    </record>

    <!-- ══════════════════════════════════════════════════════
         Qualification Master — Form
    ══════════════════════════════════════════════════════ -->
    <record id="view_edu_qualification_form" model="ir.ui.view">
        <field name="name">edu.qualification.form</field>
        <field name="model">edu.qualification</field>
        <field name="arch" type="xml">
            <form string="Qualification">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="sequence"/>
                        <field name="active"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- ══════════════════════════════════════════════════════
         Qualification Master — Action
    ══════════════════════════════════════════════════════ -->
    <record id="action_edu_qualification" model="ir.actions.act_window">
        <field name="name">Qualifications</field>
        <field name="res_model">edu.qualification</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">Create your first qualification</p>
            <p>Configure available qualifications (e.g. SLC/SEE, +2, Bachelors, Masters) for pre-admission and admission forms.</p>
        </field>
    </record>

    <!-- Menu under Configuration -->
    <menuitem id="menu_edu_qualification"
              name="Qualifications"
              parent="menu_pre_admission_configuration"
              action="action_edu_qualification"
              sequence="20"
              groups="group_pre_admission_officer,edu_academic_structure.group_education_admin"/>
</odoo>
