<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="action_hospital_patient" model="ir.actions.act_window"> <!-- same action we defined on menu.xml -->
        <field name="name">Patients</field>
        <field name="res_model">hospital.patient</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
            Create a new patient
            </p>
        </field>
    </record>

    <record id="view_hospital_patient_list" model="ir.ui.view">
         <field name="name">hospital.patient.list</field>
         <field name="model">hospital.patient</field>
         <field name="arch" type="xml">
             <list string="Patients">
                <field name="contact_id"/>
                <field name="date_of_birth"/>  <!--enables custom field to show in the list view-->
                <field name="gender"/>
            </list>
        </field>
    </record>

    <record id="view_hospital_patient_form" model="ir.ui.view">
         <field name="name">hospital.patient.form</field>
         <field name="model">hospital.patient</field>
         <field name="arch" type="xml">
             <form string="Patients">
                <header>
                    <button name="action_create_sale_order" string="Create Sale Order" type="object" class="btn-primary" invisible="sale_order_id"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_open_sale_order"
                                type="object"
                                class="oe_stat_button"     
                                icon="fa-shopping-cart"
                                invisible="not sale_order_id">
                            <field name="sale_order_id" string="Sale Order" widget="statinfo"/>
                        </button>
                    </div>
                    <group>
                        <group>
                            <field name="contact_id"/>
                            <field name="date_of_birth"/>
                            <field name="age"/>
                        </group>
                        <group>
                            <field name="gender"/>
                            <field name="service_product_id"/>
                            <field name="sale_order_id" readonly="1"/>
                            <field name="tags_id" widget = "many2many_tags"/>
                            <field name="note"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Appointments">
                            <field name="appointment_ids">
                                <list editable="bottom">
                                    <field name="reference"/>
                                    <field name="date_appointment"/>
                                    <field name="state"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <chatter />
            </form>
        </field>
    </record>

    <record id="view_hospital_patient_kanban" model="ir.ui.view">
         <field name="name">hospital.patient.kanban</field>
         <field name="model">hospital.patient</field>
         <field name="arch" type="xml">
             <kanban string="Patients">
                <field name="contact_id"/>
                <field name="gender"/>
                <field name="date_of_birth"/>
                <templates>
                    <t t-name="card">
                         <div class="oe_kanban_card oe_kanban_global_click">
                            <div class="oe_kanban_details">
                                <div>Name:<field name="contact_id"/></div>
                                <div>Gender: <field name="gender"/></div>
                                <div>DOB: <field name="date_of_birth"/></div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

</odoo>