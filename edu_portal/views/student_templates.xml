<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ══════════════════════════════════════════════════════
         Student Home — Google Classroom-style card grid.
    ══════════════════════════════════════════════════════ -->
    <template id="student_home_page" name="Student Home">
        <t t-call="edu_portal.portal_layout">
            <div class="classroom-grid-wrap">
                <h2 class="page-title">My Classrooms</h2>
                <t t-if="classroom_cards">
                    <div class="classroom-grid">
                        <t t-foreach="classroom_cards" t-as="card">
                            <t t-call="edu_portal.classroom_card_component">
                                <t t-set="classroom" t-value="card['classroom']"/>
                                <t t-set="status_label" t-value="card['status_label']"/>
                                <t t-set="status_class" t-value="card['status_class']"/>
                                <t t-set="detail_url" t-value="card['detail_url']"/>
                            </t>
                        </t>
                    </div>
                </t>
                <t t-else="">
                    <t t-call="edu_portal.empty_state_component">
                        <t t-set="icon" t-value="'📚'"/>
                        <t t-set="title" t-value="'No classrooms yet'"/>
                        <t t-set="message" t-value="'Your classrooms will appear here once your section is set up.'"/>
                    </t>
                </t>
            </div>
        </t>
    </template>

    <!-- ══════════════════════════════════════════════════════
         Student Profile
    ══════════════════════════════════════════════════════ -->
    <template id="student_profile_page" name="Student Profile">
        <t t-call="edu_portal.portal_layout">
            <h2 class="page-title">My Profile</h2>
            <div class="card">
                <p class="card-title">Name</p>
                <p class="card-value"><t t-esc="student.display_name"/></p>
            </div>
            <div class="stat-grid">
                <div class="card">
                    <p class="card-title">Student ID</p>
                    <p class="card-value" style="font-size:18px;"><t t-esc="student.student_no or '—'"/></p>
                </div>
                <div class="card">
                    <p class="card-title">Date of Birth</p>
                    <p class="card-value" style="font-size:18px;"><t t-esc="student.dob or '—'"/></p>
                </div>
            </div>
        </t>
    </template>

    <!-- ══════════════════════════════════════════════════════
         Student Fees — dues + payments + balance.
    ══════════════════════════════════════════════════════ -->
    <template id="student_fees_page" name="Student Fees">
        <t t-call="edu_portal.portal_layout">
            <h2 class="page-title">My Fees</h2>
            <div class="stat-grid">
                <div class="card">
                    <p class="card-title">Total Due</p>
                    <p class="card-value"><t t-esc="'{:,.0f}'.format(total_due)"/></p>
                </div>
                <div class="card">
                    <p class="card-title">Dues</p>
                    <p class="card-value"><t t-esc="len(dues)"/></p>
                </div>
                <div class="card">
                    <p class="card-title">Payments</p>
                    <p class="card-value"><t t-esc="len(payments)"/></p>
                </div>
            </div>

            <h3 class="page-title" style="margin-top:30px;">Dues</h3>
            <t t-if="dues">
                <table class="data-table">
                    <thead>
                        <tr>
                            <th>Fee Head</th>
                            <th>Due Date</th>
                            <th>Amount</th>
                            <th>Balance</th>
                            <th>State</th>
                        </tr>
                    </thead>
                    <tbody>
                        <t t-foreach="dues" t-as="d">
                            <tr>
                                <td><t t-esc="d.fee_head_id.name"/></td>
                                <td><t t-esc="d.due_date"/></td>
                                <td><t t-esc="'{:,.0f}'.format(d.amount)"/></td>
                                <td><t t-esc="'{:,.0f}'.format(d.balance_amount)"/></td>
                                <td><span class="classroom-card-badge badge-muted"><t t-esc="d.state.title()"/></span></td>
                            </tr>
                        </t>
                    </tbody>
                </table>
            </t>
            <t t-else=""><p style="color:var(--text-muted);">No dues recorded.</p></t>

            <h3 class="page-title" style="margin-top:30px;">Payments</h3>
            <t t-if="payments">
                <table class="data-table">
                    <thead>
                        <tr>
                            <th>Date</th>
                            <th>Reference</th>
                            <th>Amount</th>
                        </tr>
                    </thead>
                    <tbody>
                        <t t-foreach="payments" t-as="p">
                            <tr>
                                <td><t t-esc="p.payment_date"/></td>
                                <td><t t-esc="p.name"/></td>
                                <td><t t-esc="'{:,.0f}'.format(p.amount)"/></td>
                            </tr>
                        </t>
                    </tbody>
                </table>
            </t>
            <t t-else=""><p style="color:var(--text-muted);">No payments yet.</p></t>
        </t>
    </template>
</odoo>
