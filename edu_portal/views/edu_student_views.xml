<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- ══════════════════════════════════════════════════════
         edu.student — Portal Access Button
    ══════════════════════════════════════════════════════ -->
    <record id="view_edu_student_form_portal" model="ir.ui.view">
        <field name="name">edu.student.form.portal</field>
        <field name="model">edu.student</field>
        <field name="inherit_id" ref="edu_student.view_edu_student_form"/>
        <field name="arch" type="xml">
            <!-- Add portal_access field and buttons to the header -->
            <xpath expr="//header" position="inside">
                <button name="action_grant_portal_access"
                        type="object"
                        string="Grant Portal Access"
                        class="btn-primary"
                        invisible="portal_access"
                        confirm="Grant student portal access? A welcome email will be sent."
                        groups="edu_academic_structure.group_education_admin"/>
                <button name="action_revoke_portal_access"
                        type="object"
                        string="Revoke Portal Access"
                        class="btn-warning"
                        invisible="not portal_access"
                        confirm="Revoke student portal access?"
                        groups="edu_academic_structure.group_education_admin"/>
            </xpath>

            <!-- Add portal_access field to the sheet -->
            <xpath expr="//div[@name='button_box']" position="inside">
                <field name="portal_access" widget="boolean_button"
                       class="oe_stat_button" icon="fa-key"/>
            </xpath>
        </field>
    </record>
</odoo>
