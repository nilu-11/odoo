<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_edu_assessment_bulk_lock_wizard_form" model="ir.ui.view">
        <field name="name">edu.assessment.bulk.lock.wizard.form</field>
        <field name="model">edu.assessment.bulk.lock.wizard</field>
        <field name="arch" type="xml">
            <form string="Confirm / Lock Assessment Records">
                <group string="Selected Records">
                    <field name="record_ids" widget="many2many_tags" readonly="1"/>
                </group>
                <group string="Summary" invisible="not record_ids">
                    <field name="draft_count" readonly="1"/>
                    <field name="confirmed_count" readonly="1"/>
                    <field name="locked_count" readonly="1"/>
                </group>
                <group string="Action">
                    <field name="action" widget="radio"/>
                </group>
                <group invisible="not result_message">
                    <field name="result_message" readonly="1"
                           widget="char" nolabel="1"/>
                </group>
                <footer>
                    <button name="action_apply" string="Apply" type="object"
                            class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>
</odoo>
